#!/usr/bin/env node

// Direct production starter for Replit
import('./server/index.ts').then(() => {
  console.log('Server started successfully');
}).catch(error => {
  console.error('Failed to start server:', error);
  process.exit(1);
});